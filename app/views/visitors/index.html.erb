<!-- Page Content -->
<div class="container">

    <div class="row">
        <div class="col-lg-12 text-center">
            <h1><%= image_tag @header_image_filename %></h1>
            <p class="lead">Hello.</p>
            <% @strips.each do |strip| %>
              <li>
                <%= strip.title %>
              </li>
            <% end %>
        </div>
    </div>
    <!-- /.row -->

</div>
<!-- /.container -->
